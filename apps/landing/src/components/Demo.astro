---
---

<section id="demo" class="py-16 sm:py-20 bg-slate-50">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-8 sm:mb-12">
            <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-primary mb-4 px-4">Así se ven por dentro</h2>
            <p class="text-slate-600">Limpio, organizado y sin distracciones.</p>
        </div>

        <!-- Browser Mockup -->
        <div class="relative rounded-2xl shadow-2xl bg-white border border-slate-200 overflow-hidden">
            <!-- Mockup Header -->
            <div class="bg-white border-b border-slate-200 px-4 py-3 flex items-center justify-between">
                <div class="flex items-center gap-2">
                    <div class="w-3 h-3 rounded-full bg-slate-300"></div>
                    <div class="w-3 h-3 rounded-full bg-slate-300"></div>
                    <div class="w-3 h-3 rounded-full bg-slate-300"></div>
                </div>
                <div class="bg-slate-100 text-xs text-slate-500 px-3 py-1 rounded-md">apuntes-premium.com/learn/react-hooks</div>
                <div></div>
            </div>

            <!-- Mockup Content (Layout Split) -->
            <div class="flex h-[500px] sm:h-[600px] md:h-[700px]">
                <!-- Sidebar -->
                <div class="hidden md:block w-64 bg-slate-50 border-r border-slate-200 p-6 overflow-y-auto">
                    <div class="text-xs font-semibold text-slate-400 uppercase tracking-wider mb-4">Módulos</div>
                    <div class="space-y-1">
                        <div class="p-2 text-sm text-slate-700 font-medium hover:bg-slate-200 rounded cursor-pointer">Introducción</div>
                        <div class="p-2 text-sm text-secondary bg-blue-50 font-semibold rounded cursor-pointer border-l-2 border-secondary">React Hooks</div>
                        <div class="p-2 text-sm text-slate-600 hover:bg-slate-100 rounded cursor-pointer pl-6">useState</div>
                        <div class="p-2 text-sm text-slate-600 hover:bg-slate-100 rounded cursor-pointer pl-6">useEffect</div>
                        <div class="p-2 text-sm text-slate-700 font-medium hover:bg-slate-200 rounded cursor-pointer mt-2">Backend Node.js</div>
                         <div class="p-2 text-sm text-slate-700 font-medium hover:bg-slate-200 rounded cursor-pointer">Base de Datos</div>
                    </div>
                </div>

                <!-- Main Content -->
                <div class="flex-1 p-8 md:p-12 overflow-y-auto bg-white">
                    <div class="max-w-3xl mx-auto space-y-8">
                        <div>
                            <span class="text-sm font-medium text-secondary mb-2 block">Módulo 2: Frontend Moderno</span>
                            <h1 class="text-4xl font-bold text-gray-900 mb-6">Dominando React Hooks</h1> 
                        </div>

                        <div class="prose prose-slate max-w-none">
                            <p class="text-lg text-slate-700 leading-relaxed">
                                Los Hooks te permiten usar estado y otras características de React sin escribir una clase. Son funciones que te permiten "enganchar" el estado y el ciclo de vida de React desde componentes funcionales.
                            </p>

                            <div class="my-8 p-4 bg-yellow-50 border-l-4 border-yellow-400 text-yellow-800 rounded-r">
                                <strong>Nota importante:</strong> Solo puedes llamar Hooks en el nivel superior. No los llames dentro de bucles, condiciones o funciones anidadas.
                            </div>

                            <h3 class="text-2xl font-semibold text-gray-800 mt-8 mb-4">El Hook useState</h3>
                            <p class="text-slate-700 mb-4">
                                `useState` es un Hook que te permite añadir estado de React a un componente de función.
                            </p>

                            <!-- Code Block Mockup -->
                            <div class="bg-[#0F172A] rounded-lg p-6 font-mono text-sm text-slate-300 overflow-x-auto my-6 shadow-md border border-slate-800">
                                <div class="flex justify-between items-center mb-4 text-xs text-slate-500 border-b border-slate-700 pb-2">
                                    <span>Counter.jsx</span>
                                    <span>jsx</span>
                                </div>
                                <pre class="whitespace-pre-wrap"><code><span class="text-purple-400">import</span> React, &#123; useState &#125; <span class="text-purple-400">from</span> <span class="text-green-400">'react'</span>;

<span class="text-purple-400">function</span> <span class="text-blue-400">Counter</span>() &#123;
  <span class="text-slate-500">// Declara una nueva variable de estado, la llamaremos "count"</span>
  <span class="text-purple-400">const</span> [count, setCount] = <span class="text-blue-400">useState</span>(<span class="text-orange-400">0</span>);

  <span class="text-purple-400">return</span> (
    &lt;<span class="text-red-400">div</span>&gt;
      &lt;<span class="text-red-400">p</span>&gt;Hiciste click &#123;count&#125; veces&lt;/<span class="text-red-400">p</span>&gt;
      &lt;<span class="text-red-400">button</span> <span class="text-blue-300">onClick</span>=&#123;() =&gt; <span class="text-blue-400">setCount</span>(count + <span class="text-orange-400">1</span>)&#125;&gt;
        Hazme click
      &lt;/<span class="text-red-400">button</span>&gt;
    &lt;/<span class="text-red-400">div</span>&gt;
  );
&#125;</code></pre>
                            </div>
                            
                            <h3 class="text-2xl font-semibold text-gray-800 mt-8 mb-4">¿Cuándo usar useEffect?</h3>
                            <p class="text-slate-700 mb-4">
                                El Hook <code class="px-2 py-1 bg-slate-100 rounded text-sm">useEffect</code> te permite realizar efectos secundarios en componentes funcionales. Úsalo cuando necesites:
                            </p>
                            
                            <ul class="space-y-2 text-slate-700 mb-6">
                                <li class="flex items-start gap-2">
                                    <span class="text-secondary font-bold">•</span>
                                    <span>Hacer llamadas a APIs al montar el componente</span>
                                </li>
                                <li class="flex items-start gap-2">
                                    <span class="text-secondary font-bold">•</span>
                                    <span>Suscribirte a eventos del navegador (scroll, resize)</span>
                                </li>
                                <li class="flex items-start gap-2">
                                    <span class="text-secondary font-bold">•</span>
                                    <span>Actualizar el título del documento</span>
                                </li>
                                <li class="flex items-start gap-2">
                                    <span class="text-secondary font-bold">•</span>
                                    <span>Sincronizar datos con localStorage</span>
                                </li>
                            </ul>
                            
                            <div class="p-4 bg-blue-50 rounded-lg border-l-4 border-secondary mb-6">
                                <p class="text-sm font-semibold text-secondary mb-2">💡 Tip profesional:</p>
                                <p class="text-sm text-slate-700">
                                    Siempre limpia tus efectos retornando una función de cleanup para evitar memory leaks.
                                </p>
                            </div>
                            
                            <h3 class="text-2xl font-semibold text-gray-800 mt-8 mb-4">Ejemplo práctico</h3>
                            <p class="text-slate-700 mb-4">
                                Vamos a crear un hook que hace fetch de datos cuando el componente se monta:
                            </p>
                            
                            <div class="relative">
                                <div class="absolute inset-0 bg-gradient-to-b from-transparent via-white/50 to-white z-10"></div>
                                <div class="bg-[#0F172A] rounded-lg p-6 font-mono text-sm text-slate-300 overflow-hidden opacity-60">
                                    <div class="flex justify-between items-center mb-4 text-xs text-slate-500 border-b border-slate-700 pb-2">
                                        <span>UserProfile.jsx</span>
                                        <span>jsx</span>
                                    </div>
                                    <pre class="whitespace-pre-wrap"><code><span class="text-purple-400">function</span> <span class="text-blue-400">UserProfile</span>(&#123; userId &#125;) &#123;
  <span class="text-purple-400">const</span> [user, setUser] = <span class="text-blue-400">useState</span>(<span class="text-orange-400">null</span>);
  <span class="text-purple-400">const</span> [loading, setLoading] = ...</code></pre>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
             <!-- Fade Overlay -->
            <div class="absolute bottom-0 left-0 w-full h-40 bg-gradient-to-t from-slate-50 to-transparent flex items-end justify-center pb-8">
                 <a href="#pricing" class="px-6 py-3 bg-primary text-white font-bold rounded-lg shadow-lg hover:bg-slate-800 transition-colors">
                    Desbloquear todo el contenido
                </a>
            </div>
        </div>
    </div>
</section>
